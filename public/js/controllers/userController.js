'use strict';

angular.module("controllers.userController", [])
.controller("userController", ["$rootScope", "$scope", "$window", "$location",
	function($rootScope, $scope, $window, $location){
		console.log("Here in userController");
	}	
]);
